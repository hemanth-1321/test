"use strict";

const calculator = require("./calculator");

/**
 * Expose the calculator module for external use.
 *
 * @module calculator
 */
module.exports = calculator;

/* Example usage:
const calc = require("./index");
console.log(calc.add(5, 7)); // 12
*/