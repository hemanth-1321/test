from calculator import add, multiply

def sum(a, b):
    """Prints the sum of a and b using the add function from calculator."""
    result = add(a, b)
    print(result)

def mutlipy(a, b):
    """Returns the product of a and b using the multiply function from calculator."""
    return multiply(a, b)