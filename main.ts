import { modulo } from "./helper";

export function multiply1(a: number, b: number) {
  console.log(a * b);
}

function sum(a: number, c: number) {
  console.log(a + c);
}

sum(4, 5);
multiply1(5, 3);

sum(9, 0);

sum(4, 5);
multiply1(5, 3);