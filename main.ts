import { mod } from "./helper";

function sum(a: number, c: number) {
  console.log(a + c);
}

function multiply(a: number, c: number) {
  console.log(a + c);
}

const res = mod(9, 8);
console.log(res);

sum(4, 5);
multiply(5, 3);
